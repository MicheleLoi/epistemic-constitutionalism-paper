# Obsidian + published output
.obsidian/
published/

# 06_conversations/ — created automatically by MHC export hook (hardcoded in script).
# Not a project folder. Gitignored entirely; ground truth is MHC-prototype/00_full_conversations/.
06_conversations/

# Claude Code / local settings + tmp working dirs
.claude/
tmpclaude-*
tmpclaude-*/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env


# Conversation transcripts — kept local, not part of public materials (untracked 2026-06-02).
# `conversations/` is the export hook's actual project dir; transcripts now live only in MHC-W,
# so it must be ignored too (it was the unignored gap that nearly committed transcripts).
00_conversations_full/
conversations/
