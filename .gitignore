# Obsidian + published output
.obsidian/
published/

# 06_conversations/ — created automatically by MHC export hook (hardcoded in script).
# Not a project folder. Gitignored entirely; ground truth is MHC-prototype/00_full_conversations/.
06_conversations/

# Claude Code / local settings + tmp working dirs
.claude/
tmpclaude-*
tmpclaude-*/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env


# Conversation transcripts policy — see 09_notes/_decision_conversations_public_until_arxiv.md
#
# 00_conversations_full/ — TRACKED PUBLICLY until next arXiv version (reverses 2026-06-02
# untracking; files were already public in git history from e33d351 onward, so the
# privacy regression was nominal). At next arXiv submission: re-add the ignore line +
# `git rm -r --cached 00_conversations_full/` to untrack without deleting.
#
# conversations/ remains ignored — export hook's project dir; per 2026-06-03 decision
# (decision_conversations_only_mhcw_20260603.md) all future exports go to MHC-W archive only.
conversations/

# one-time migration backup (contains a stray file from another project) — not for publication
.adapt-migration-backup-2026-06-02/
# stray cwd-marker junk anywhere in the tree
**/tmpclaude-*
